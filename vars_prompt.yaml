- name: varibles from frompt
  hosts: local
  connection:  local
  vars_prompt:

    - name: username
      prompt: enter the user name
      private: false

    - name: password
      prompt: enter the password
      private: false

  tasks: # list of tasks
    - name: printing user information
      ansible.builtin.debug:
         msg: "user name is {{ username }} and password is {{ password }}"